import { SnsIcons } from '../../../components/atoms/icon/sns/SnsIcons';

export const FooterSnsList = (): JSX.Element => {
  return (
    <nav className='flex items-center gap-4'>
      <SnsIcons />
    </nav>
  );
};
